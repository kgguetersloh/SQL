STEP 1
SELECT *
FROM superstore 
ORDER BY price;

STEP 2
SELECT AVG(price)
FROM SUPERSTORE

STEP 3
SELECT SUM(price), category
FROM superstore
WHERE category = "Kitchen Supplies"

STEP 4
What is the total amount of furnishings in the store?
SELECT SUM(stock_quantity), category
FROM superstore
WHERE category = "Furnishings"       
ANSWER: 175 furnishings in the store
